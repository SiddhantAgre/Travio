<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Travio</title>
  </head>
  <body>
    <h3>All Listing</h3>
    <form action="/listings/new">
      <button>Add new listing</button>
    </form>
    <ul>
      <% for(listing of listings) { %>
      <li><a href="/listings/<%= listing._id %>"><h4><%= listing.title %></h4></a></li>
      <% } %>
    </ul>
  </body>
</html>
